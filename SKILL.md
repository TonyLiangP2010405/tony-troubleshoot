---
name: tony-troubleshoot
description: "根据用户提供的 API 命令文档，对全部在线 WyreStorm 设备进行穷尽式排查，保存原始证据、命令与字段覆盖情况以及可断点续查的本地检查点。适用于 WyreStorm API 设备清单、健康检查、配置与状态采集和故障排查审计；未提供适用的 API 命令文档时不得使用。"
---

# WyreStorm 全量排查

对排查范围内的每一台在线设备进行可复现的信息快照采集，并获取用户所提供 API 文档允许读取的全部信息。必须保存足够的本地状态，使排查在模型切换、任务中断或上下文压缩后可以继续进行，而且不会重复已经完成的工作。

## 强制前置条件

连接任何设备之前，必须要求用户提供：

1. 排查范围内每种型号、系列以及固件/API 版本所对应的准确 API 命令文档。优先使用本地文件，也可以接受 URL 或用户粘贴的完整文档。
2. 管理端点或发现设备时使用的起始地址、连接方式，以及读取设备所需的凭据或已认证会话。
3. 本次排查使用的本地案件目录，以及对目标网络和设备范围的确认。

不得凭记忆推测命令，也不得使用其他型号的手册代替。如果文档不完整、型号或版本不匹配、无法访问或者存在缺页，必须停止采集并明确指出文档缺口。在缺口解决以前，不得声称已经实现全量覆盖。

开始新案件时使用 `scripts/Initialize-WyreStormCase.ps1`。该脚本要求至少提供一份 API 文档，并创建可长期保存的案件目录结构。如果脚本无法运行，则必须在发送第一条设备命令之前，手动创建下文规定的等价文件。

每完成一个采集单元并更新账本后，使用 `scripts/Update-WyreStormCheckpoint.ps1` 重新统计数量，原子更新 `checkpoint.json` 和 `session-handoff.md`，并追加检查点事件。调用该脚本之前，进度账本和原始证据必须已经写入磁盘。

## 安全边界

- 只有当用户提供的文档能够证明命令是只读查询时，才可以自动执行该命令。
- 写入、路由切换、重启、重置、升级、上传、清除、测试图案以及其他会改变设备状态的命令，必须加入命令目录，但不得执行；只有用户另行明确要求并授权相应操作后才能执行。
- 遵守文档规定的速率限制、命令封装格式、认证、会话、分页和连接规则。优先选择干扰最小且文档明确支持的访问方式。
- 不得将密码、令牌、私钥或完整会话 Cookie 写入案件文档。保留字段名，并将字段值写为 `[REDACTED]`；同时在字段覆盖审计中登记该脱敏行为，防止它被误判为漏采。

## 必须长期保存的案件文件

以下本地文件是排查进度的唯一事实来源，不能依赖对话记忆：

- `source/api-docs/` 和 `source/api-doc-index.csv`：API 文档的完整副本或 URL 记录、可用时的文件哈希，以及文档适用的型号和版本。
- `command-catalog.csv`：每一条文档命令占一行，包括非只读命令、参数、响应字段、分页方式、适用范围、文档位置和执行分类。
- `device-inventory.csv`：所有已发现设备和子设备，包括稳定设备 ID、端点、父设备或控制器、型号、序列号、固件、在线状态、发现证据和扫描状态。
- `progress-ledger.csv`：每个 `(device_id, command_id, 标准化参数集合)` 占一行，包括状态、尝试次数、时间戳、证据路径、摘要和错误。
- `coverage-audit.csv`：记录文档预期字段与实际响应字段，以及所有缺失、条件性、已脱敏、不支持或额外字段。
- `raw/`：保存每次尝试的逐字响应内容和请求元数据，但必须对凭据和机密值进行脱敏。文件按稳定设备 ID、命令 ID 和标准化参数集合组织；不得覆盖先前尝试，并且每个脱敏字段都必须登记在字段覆盖审计中。
- `findings.md`：附带证据路径的观察和排查假设；必须把已确认事实与解释性判断分开。
- `checkpoint-log.md`：追加式日志；每完成一个工作单元以及每次设备清单发生变化后都要写入，不得改写历史记录。
- `session-handoff.md`：简明记录当前进度、最后完成项、下一未完成项、阻塞因素和准确续查步骤。
- `checkpoint.json`：机器可读取的案件状态，包括数量、时间戳、文档指纹、最后完成记录和下一步操作。
- `final-report.md`：最终完成状态、设备清单、排查发现、未解决缺口和覆盖声明。

`progress-ledger.csv` 中的状态值固定为 `not_started`、`in_progress`、`completed`、`failed`、`unsupported` 或 `blocked`。只有 `completed` 表示已经获取文档规定的响应并完成字段审计。查询失败、不支持、权限不足、响应格式错误或超时，都必须作为明确的覆盖缺口保留。

## 开始工作前必须恢复进度

每个会话开始时，以及发生模型切换或上下文变化后，必须先执行以下操作：

1. 读取 `session-handoff.md`、`checkpoint.json`、API 文档索引、命令目录、设备清单、进度账本、字段覆盖审计，以及 `checkpoint-log.md` 的末尾部分。
2. 将所有 `in_progress` 行与对应原始尝试文件进行核对。只有响应采集和字段审计均已完成时，才能将其标记为 `completed`；否则保留已有尝试，并继续处理或使用新的尝试编号重试只读命令。
3. 验证 API 文档指纹和目标范围没有发生变化。
4. 选择第一个适用且尚未 `completed` 的记录。不得仅仅因为对话上下文丢失而重新执行已完成记录。

如果文档、固件、目标范围或设备身份发生变化，必须登记变化，并且只让受影响的命令目录行或进度行失效。不得丢弃仍然有效的既有证据。

## 全量排查流程

### 1. 完整建立文档命令目录

读取用户提供文档的每一页和每个章节，包括附录、参数表、响应结构、条件字段、型号或固件说明、分页规则和传输细节。广泛执行设备命令之前，先将每一条命令加入 `command-catalog.csv`。为命令分配稳定 ID，并将其分类为只读、会改变状态、含义不明确或不适用。含义不明确的命令必须保持阻塞，直到文档或用户解决歧义。

对于每一条可读命令，根据文档和已发现的设备能力，穷举所有有限参数取值，包括全部端口、输入、输出、编码器、解码器、码流、网络接口、插槽、区域、通道、页码以及其他文档选择器。除非文档明确说明汇总命令与详细命令返回的信息完全相同，否则便利性的汇总命令不能代替详细命令。

### 2. 发现完整在线设备清单

只能使用文档规定的发现、清单或状态命令。首先保存发现命令的原始响应，然后优先使用序列号、MAC 地址或设备 UUID 建立稳定设备 ID；不得只使用可变的显示名称。API 如果公开了控制器、机箱、板卡、端点、发送器、接收器或其他子设备，必须递归展开并纳入清单。

离线设备也要写入设备清单，但只有已经确认在线且属于排查范围的设备才执行全量采集。在收尾阶段重新执行发现流程。如果发现新上线或之前未发现的设备，先完成这些设备的扫描，再次执行发现；直到连续两次发现都没有未扫描的在线设备为止。每次发现都要记录时间戳和证据路径。

### 3. 建立执行矩阵

在条件允许时，执行前先建立所有适用的 `(设备, 命令, 参数集合)` 行。参数键必须以确定性方式标准化，例如 `input=01;output=03;page=0002`，以便可靠续查和识别重复任务。如果响应中出现新的设备、能力、选择器或分页，必须立即扩展设备清单、命令适用范围和执行矩阵。

### 4. 逐单元采集并保存检查点

对执行矩阵中的每一行执行以下步骤：

1. 发送命令之前，将该行状态设为 `in_progress` 并写入检查点。
2. 保存请求元数据，包括带时区的时间戳、端点、传输方式、命令 ID、文档规定的命令形式、标准化参数、尝试编号、超时时间和文档引用。所有机密值必须脱敏。
3. 执行文档规定的只读命令，逐字保存响应且不得覆盖以前的尝试；只允许对凭据和机密值脱敏，并要在字段覆盖审计中登记这些字段。必须穷尽所有页、游标和文档规定的记录范围。
4. 将响应与文档中的每个字段进行比较，包括条件字段和嵌套字段。在 `coverage-audit.csv` 中记录缺失、空值、条件性、已脱敏、意外出现和文档未定义的字段；不得在标准化处理时静默丢弃这些信息。
5. 更新进度行，并在需要时更新排查发现；然后调用 `scripts/Update-WyreStormCheckpoint.ps1`，传入已完成记录、下一步操作和日志摘要。完成这些写盘操作后，才能继续下一行。

重试次数必须有上限，并符合文档规定和设备负载承受能力。每次重试都要使用独立证据文件和账本尝试记录。如果某个命令或字段始终无法访问，必须保留失败证据并将案件标记为未完成，不得猜测或省略该信息。

### 5. 完成前证明覆盖情况

只有同时满足以下全部条件，才能宣布排查完成：

- 每一份用户提供的文档都已建立索引，并且所有适用页面和章节都已编入命令目录。
- 每一条文档命令都有明确处理结果；对每台在线且适用的设备以及每组有限参数值，每一条适用的只读命令都有对应执行矩阵行。
- 所有分页、游标和文档规定的范围都已穷尽。
- 每个实际响应都已保存，并且所有预期字段都完成审计。
- 收尾阶段连续两次发现均未出现尚未扫描的在线设备。
- 不存在 `not_started` 或 `in_progress` 行，并且每个 `failed`、`unsupported`、`blocked` 或字段缺失记录都已列为未解决的信息缺口。

只要仍有任何缺口，结果必须标记为 **部分完成/未完成**。“已经尝试获取全部可用信息”不等于“已经成功获取全部信息”。

## 报告要求

`findings.md` 必须先写结论，并为每项结论提供证据路径。如果原始证据与摘要冲突或不足以支持摘要，不得仅凭摘要作出诊断。`final-report.md` 必须说明采集时间范围和时区、文档与版本指纹、在线设备数量、命令和参数总数、完成/失败/阻塞数量、字段覆盖情况、最后两次设备发现结果、脱敏情况以及未解决缺口。报告必须包含准确的本地证据路径，以便其他 agent 审计并继续案件。
